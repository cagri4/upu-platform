/**
 * /tr/* top-level locale loading state — catch-all.
 * Banking skeleton full-page (spinner kaldırıldı — Çağrı 2026-05-27).
 */
import { SkeletonFullPage } from "@/components/banking";

export default function LocaleLoading() {
  return <SkeletonFullPage />;
}
