/**
 * /tr/(otel-panel)/* loading state — Otel route group level.
 * Skeleton screen (spinner kaldırıldı — Çağrı 2026-05-27).
 */
import { SkeletonPanelShell } from "@/components/banking";

export default function OtelPanelLoading() {
  return <SkeletonPanelShell />;
}
